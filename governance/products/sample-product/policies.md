# sample-product Governance

Adds to (never relaxes) [global governance](../../global/). Applies to
every domain within sample-product unless a domain layer adds further
rules on top.

## Rules

1. **Rate limiting on public endpoints** — Public-facing endpoints must enforce a rate limit to prevent abuse.
